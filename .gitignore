__pycache__
*.so
build
.coverage_*
*.egg-info
*~
*.swp
slurm*
logs
.vscode
local/
.gitmodules
wandb/
onelogger.log
onelogger.err
.venv
runs/
/test_cases/
**/dist/

# Sphinx documentation
docs/_build
docs/apidocs

# Git worktrees
.worktrees/
.claude/worktrees/
.slurm-bridge/

# Local secrets — real keys live in ../secrets.sh (outside the repo). This guards
# against a copy landing in the repo; secrets.sh.example (template) stays tracked.
secrets.sh
